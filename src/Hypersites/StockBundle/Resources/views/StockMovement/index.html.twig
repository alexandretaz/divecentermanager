{% extends '::base.html.twig' %}

{% block body -%}
    <h1>StockMovement list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Kindmovement</th>
                <th>Movedby</th>
                <th>Description</th>
                <th>Movementdate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('stock_movement_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.kindMovement }}</td>
                <td>{{ entity.movedBy }}</td>
                <td>{{ entity.description }}</td>
                <td>{% if entity.movementDate %}{{ entity.movementDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('stock_movement_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('stock_movement_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('stock_movement_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
