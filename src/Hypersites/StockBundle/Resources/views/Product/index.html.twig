{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Product List</h1>
    <form class="form-inline">
        <div class="form-group">
            <label for="exampleInputName2">Product</label>
            <input type="text" class="form-control" id="exampleInputName2" placeholder="Type the product name   ">
        </div>
        <div class="form-group">
            <label for="exampleInputEmail2">Supplier or Brand</label>
            <input type="email" class="form-control" id="exampleInputEmail2" placeholder="Type The Suplier or Brand">
        </div>
        <button type="submit" class="btn btn-default">Filter (not working yet)</button>
    </form>
    <form class="form-inline" action="{{ path('stock_product_new') }}">
        <button type="submit" class="btn btn-info">Add a new Product</button>
    </fom>
    
    <table class="records_list table table-bordered table-responsive table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Mincost</th>
                <th>Maxcost</th>
                <th>Supplierean</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('stock_product_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.minCost }}</td>
                <td>{{ entity.maxCost }}</td>
                <td>{{ entity.supplierEan }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('stock_product_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('stock_product_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('stock_product_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
